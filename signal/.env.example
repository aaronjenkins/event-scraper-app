# Copy to .env before `docker compose up`.

# The Signal account number the sender links to / sends as, E.164 format.
SIGNAL_NUMBER=+15551234567

# Shared secret callers must present as api_key. Must match the app's
# SIGNAL_WEBHOOK_KEY. Leave blank to disable the check (not recommended).
SIGNAL_WEBHOOK_KEY=changeme
