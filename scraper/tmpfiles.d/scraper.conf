# Keep scraper cache entries fresh. Files older than 1 day are removed.
# The scheduled runs overwrite latest.html every morning; this rule only
# kicks in if a site's timer stops firing.
d /var/cache/eventscraper 0755 eventscraper eventscraper 1d
